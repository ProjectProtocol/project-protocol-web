type ResourceLink = {
  contentTypeId: 'resourceLink'
  fields: {
    category: string[]
    location: string
    organization: string
    title: string
    url: string
  }
}

export default ResourceLink
